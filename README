1. Set up

First, make sure you have Python and pip installed on your system "pip install -r requirements.txt"
Then, run the application using "python app.py". This will start the server and you can access 
the application in your web browser at http://172.16.131.183:5000



